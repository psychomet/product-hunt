<!DOCTYPE html>
<html>
<head>
    <meta charset="utf-8">
    <title>Order Confirmation</title>
    <style>
        body {
            font-family: Arial, sans-serif;
            color: #333;
            line-height: 1.6;
        }
        .container {
            max-width: 600px;
            margin: 0 auto;
            padding: 20px;
        }
        .header {
            background-color: #f8f9fa;
            padding: 15px;
            text-align: center;
            border-radius: 5px;
        }
        .order-details {
            margin: 20px 0;
            padding: 15px;
            border: 1px solid #ddd;
            border-radius: 5px;
        }
        .footer {
            text-align: center;
            margin-top: 20px;
            font-size: 12px;
            color: #777;
        }
        table {
            width: 100%;
            border-collapse: collapse;
        }
        th, td {
            padding: 10px;
            text-align: left;
            border-bottom: 1px solid #ddd;
        }
        th {
            background-color: #f2f2f2;
        }
    </style>
</head>
<body>
    <div class="container">
        <div class="header">
            <h1>Order Confirmation</h1>
            <p>Thank you for your order!</p>
        </div>
        
        <div class="order-details">
            <h2>Order #{{ order.code }}</h2>
            <p>Order Date: {{ formatDate order.orderPlacedAt }}</p>
            
            <h3>Items Ordered</h3>
            <table>
                <thead>
                    <tr>
                        <th>Product</th>
                        <th>Quantity</th>
                        <th>Price</th>
                    </tr>
                </thead>
                <tbody>
                    {{#each order.lines }}
                    <tr>
                        <td>{{ productVariant.name }}</td>
                        <td>{{ quantity }}</td>
                        <td>{{ formatMoney linePriceWithTax }}</td>
                    </tr>
                    {{/each}}
                </tbody>
                <tfoot>
                    <tr>
                        <td colspan="2"><strong>Subtotal</strong></td>
                        <td>{{ formatMoney order.subTotalWithTax }}</td>
                    </tr>
                    <tr>
                        <td colspan="2"><strong>Shipping</strong></td>
                        <td>{{ formatMoney order.shippingWithTax }}</td>
                    </tr>
                    <tr>
                        <td colspan="2"><strong>Total</strong></td>
                        <td><strong>{{ formatMoney order.totalWithTax }}</strong></td>
                    </tr>
                </tfoot>
            </table>
            
            <h3>Shipping Address</h3>
            <p>
                {{ order.shippingAddress.fullName }}<br>
                {{ order.shippingAddress.streetLine1 }}<br>
                {{#if order.shippingAddress.streetLine2 }}
                {{ order.shippingAddress.streetLine2 }}<br>
                {{/if}}
                {{ order.shippingAddress.city }}, {{ order.shippingAddress.province }}<br>
                {{ order.shippingAddress.postalCode }}<br>
                {{ order.shippingAddress.country.name }}
            </p>
        </div>
        
        <div class="footer">
            <p>If you have any questions about your order, please contact our customer service.</p>
            <p>&copy; {{ formatDate order.orderPlacedAt 'YYYY' }} Bigi Shop. All rights reserved.</p>
        </div>
    </div>
</body>
</html> 